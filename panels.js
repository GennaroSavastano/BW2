const shortArrow = document.querySelector(".short-arrow");
const leftSidebar = document.querySelector("leftsidebar");

function arrowRight(event) {
  console.log(event);
}

//shortArrow.addEventListener("click", callback);
